import React from "react";

function Header() {
    return (
      <Alert key={primary} className="header" variant={primary}>
       <h1>Jack Kelly</h1>
      </Alert>
    );
}

export default Header;